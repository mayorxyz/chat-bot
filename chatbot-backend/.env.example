# Copy this file to .env and fill in real values

# Server port
PORT=3001

# Allowed frontend origin(s) for CORS. Comma-separate for multiple origins.
FRONTEND_URL=http://localhost:3000

# Minimum node-nlp confidence (0-1) required to accept an intent match
CONFIDENCE_THRESHOLD=0.6
